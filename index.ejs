<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aplicação Veiculos</title>
      
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0;
            width: 100vw;
            height: 100vh;
            background: linear-gradient(155deg, #FDD690 7%, #FFFDFD 50%, #9FDEDC 93%);
        }
        header{
            display: flex;
            margin-bottom: 1px solid black;
            width: 100vw;
            justify-content: space-evenly;
            
        }
        main{
            display: flex;
            flex-direction: row;
            width: 100vw;
            height: auto;
            justify-content: space-evenly;
            align-items: center;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        ul {
            list-style-type: none;
            padding: 0;
            display: flex;
            flex-wrap: wrap;
            justify-content: space-evenly;
        }

        li {
            background: linear-gradient(155deg, #FDD690 7%, #FFFDFD 50%, #9FDEDC 93%);
            border-radius: 4px;
            margin-bottom: 10px;
            padding: 10px;
            display: flex;
            align-items: center;
            flex-direction: column;
            border: 1px solid black;
            width: 200px; /* Largura fixa para cada tabela */
            margin-right: 10px; /* Espaçamento entre as tabelas */
            box-shadow: 10px 3px 23px 0px rgba(255,255,255,0.72);
        }
        p {
            margin: 0;
            font-size: 20px;
        }

        form {
            margin-top: 20px;
        }

        input, button {
            padding: 10px;
            margin-bottom: 10px;
        }

        button {
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            margin-top: 10px;
        }
        #excluir{
            background-color: red;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <header>
        <h1>Aplicação Carros</h1>

        <form id="myForm" action="/" method="POST">
            <input type="text" name="fabricante" placeholder="Fabricante" required />
            <input type="text" name="modelo" placeholder="Modelo" required>
            <input type="text" name="ano" placeholder="Ano" required>
            <input type="text" name="placa" placeholder="Placa" required>
          
            <button type="submit">Adicionar</button>
          </form> 
    </header>
    <main>
  <ul>
    <% veiculos.forEach((veiculo) => { %>
    <li>
      <h4>Fabricante</h4>
      <p><%= veiculo.fabricante %></p>
      <h4>Modelo</h4>
      <p><%= veiculo.modelo %></p>
      <h4>Ano</h4>
      <p><%= veiculo.ano %></p>
      <h4>Placa</h4>
      <p><%= veiculo.placa %></p>
      <button type="submit" id="editar">Editar</button> 
      <button type="submit" id="excluir">Excluir</button>
    </li>
    <% }); %>
  </ul>
</main>
</body>
</html>
